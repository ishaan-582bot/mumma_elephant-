/**
 * Shared utility classes and helpers.
 */

/**
 * Premium form input class string — cream-tinted background against white cards,
 * simplified focus ring, comfortable touch target.
 * Use on <input>, <select>, and <textarea> elements within card-based forms.
 */
export const inputClassName =
  'w-full rounded-[var(--radius-sm)] border border-[var(--cream-dark)] bg-[var(--cream)] px-3.5 py-3 text-sm font-medium text-[var(--text-primary)] outline-none transition-all duration-150 focus:border-[var(--blush-dark)] focus:ring-2 focus:ring-[var(--blush)] focus:ring-opacity-40';
